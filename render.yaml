services:
  - type: web
    name: legal-case-management
    env: node
    buildCommand: npm install && node build-render.cjs
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production