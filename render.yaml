services:
  - type: web
    name: legal-case-management
    env: node
    buildCommand: npm install && ./build-render.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production